package enums

// Base16Method is the method used for generating base16 colors.
//
// ENUM(static, dynamic).
type Base16Method uint

// PreviewFormat is a image format used for generating colors.
//
// ENUM(jpg, jpeg, png, webm).
type PreviewFormat uint
